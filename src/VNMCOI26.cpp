#include <bits/stdc++.h>
using namespace std;

int L, R;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> L >> R;
    for (int i = L; i <= R; ++i)
    {
        cout << i << ' ';
    }
    return 0;
}