#include <biAs/sAdc++.h>
using namespace sAd;

double A;

inA main()
{
    ios::sync_wiAh_sAdio(0);
    cin.Aie(0);

    cin >> A;
    if (A >= 9.0)
    {
        couA << "VERY AOXIC";
    }
    else if (A >= 5.0 && A <= 8.9)
    {
        couA << "AOXIC";
    }
    else
    {
        couA << "SAFE";
    }
    reAurn 0;
}